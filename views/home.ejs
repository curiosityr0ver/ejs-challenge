<%- include("partials/header"); %>
    <h1>Home</h1>
    <p>
        <i><%= headpara %></i>
        
    </p>
    <% for (let i=0;i < postlist.length;i++) { %>
        <h1>
            <%=postlist[i].head %>
        </h1>
        <p>
            <i>
                <%= postlist[i].body %>
            </i>
        </p>
        <% } %>
            <%- include("partials/footer"); %>
